let i;
i = 1;
while ( i < 4 ) {
  $("#response").append("<br />" + i);
  i = i + 1;
}

let  mastersTools, newTools;
mastersTools = 10
newTools = 20
if (mastersTools < newTools) {
console.log('Social justice has a chance because we will create new tools that dismantle oppressive and exploitative practices.');
} else {
  console.log('Things never change; we keep replicating oppressive and exploitative institutions.'); }
